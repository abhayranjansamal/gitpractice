git init
git log
git status
git status -s
git log --oneline
git add .
git commit -m "my first comment"
git commit -a -m "for modified file"
git branch  #to check created branches
git branch hotfix
git checkout hotfix
git checkout master
git merge hotfix
git remote rm origin
git remote get-url origin
git remote add origin <url>
git push origin master
git reset HEAD-2 #undo  last 2 commit keep changes in stage
git reset --hard HEAD-2 #undo  last 2 commit discard changes
git  branch -b features
git cheackout master
git pull
git branch -d features
git pull --rebase #to ignore git merge commit on remote repo
git pull -r
git confict for same file and edited by 2 developer for same line
need to resolve the conflict manually 
-git ignore
git rm -r --cache foldernamme
-- it will remove from remote repo as well
git add.
git commit -m "comment"
#you cant checkout to other branch with non commited fie
you need to stash
git stash
git stash pop
git checkout a has
git reset delete the old commit 
git revert create a new comit to discard the previous commit
git merge master 
git checkout master
git tag v1.0
git tag
git tag -d v1 v2
git push origin --tags
git push origin (tag name)
git push origin -d (tag name)
git show (tag name)
